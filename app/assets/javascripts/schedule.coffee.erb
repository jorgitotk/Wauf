@select_time = (obj, col, row) ->
	$(obj).toggleClass("green");
	is_checked_days = $("#dayc-" + col + "-r-" + row).prop("checked");
	is_checked_hours = $("#hourc-" + col + "-r-" + row).prop("checked");
	if is_checked_days
		$("#dayc-" + col + "-r-" + row).prop("checked", false)
	else
		$("#dayc-" + col + "-r-" + row).prop("checked", true)
	if is_checked_hours
		$("#hourc-" + col + "-r-" + row).prop("checked", false)
	else
		$("#hourc-" + col + "-r-" + row).prop("checked", true)

@set_courses = ->
  id = $('#faculty-type-select option:selected').val()
  if id == '' then id = 0
  $.ajax 'schedules/' + id + '/set_courses',
    type: 'POST'
    dataType: 'script'